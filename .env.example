# Pipeline Simulator -- Environment Variables

# Supabase (not needed for static demo)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Claude API (not needed for static demo)
CLAUDE_API_KEY=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_ENVIRONMENT=development

# Webhook connections (empty during initial build)
WEBHOOK_CONNECTIONS={}
